#include <iostream>

using namespace std;


class Node{
  public:
    int data;
    Node *next;
};


Node *head;

void init(){
    head = NULL;
}


bool isEmpty(){
    if (head == NULL)
        return 1;
    return 0;
}


void tambahDepan(int databaru)
{
    Node *baru;
    baru = new Node();
    baru->data = databaru;
    baru->next = baru;

    if (isEmpty())
    {
        head = baru;
        head->next = head;
    }
    else
    {
        Node *bantu;
        bantu = head;
        while (bantu->next != head)
        {
            bantu = bantu->next;
        }
        baru->next = head;
        head = baru;
        bantu->next = head;
    }
}


void tambahBelakang(int databaru2){
 Node *baru2;
    baru2 = new Node();
    baru2->data = databaru2;
    baru2->next = baru2;

    if (isEmpty())
    {
        head = baru2;
        head->next = head;
    }
    else
    {
        Node *bantu;
        bantu = head;
        while (bantu->next != head)
        {
            bantu = bantu->next;
        }
        baru2->next = head;
        head = baru2;
        bantu->next = head;
    }
	
	
	
}

void cetak(){
    if (!isEmpty()){
        Node *bantu;
        bantu = head;
        do{
            cout<<bantu->data<<" ,";
            bantu=bantu->next;
        }while (bantu!=head);
    } else {
        cout<<"List Kosong";
    }
}


void cetak2(char data3[10],int x){
	int  databalik;
    for (int i = 0; i < x; i++)
    {
 cout <<data3[i]<<" ,";
 databalik=data3[i];
 	for (int i = 0; i >x ; i--){
 		cout <<databalik<<endl;
	 }
 
    	}
}





int main()
{
   init();
char data3[10];
    int n, data,data2,hapus1,hapus2;
    cout<<"Masukan data node"<<endl;
    cout<<"banyak data node :";
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cout<<"data : ";
        cin >>data;
        tambahDepan(data);
    }
    cetak();
    int x;
    cout<<endl<<endl;
    cout<<"Menambah Node depan"<<endl;
    cout<<"banyak data yang akan di tambah :";
    cin>>n;
    for (int i = 0; i < n; i++)
    {
        cout<<"data : ";
        cin >> data2;
        tambahBelakang(data2);
    }
    cetak(); 
      cout<<endl<<endl;
      cout << "masukkan node belakang " << endl;
    cout<<"banyak data yang akan di tambah :";
    cin>>x;
       for (int i = 0; i < x; i++)
    {
    	cout << "data : ";
    	cin >>data3[i];}
    cetak();	
	cetak2(data3,x);
	   cout<<endl<<endl;
      cout << " Menyisipkan Node di posisi tertentu " << endl;
	
    return 0;
}
